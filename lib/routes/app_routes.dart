/// Route name constants — single source of truth for all navigation strings.
abstract class AppRoutes {
  static const String dashboard = '/dashboard';
  // Future routes:
  // static const String teamDetail  = '/team/:id';
  // static const String topScorers  = '/scorers';
}
